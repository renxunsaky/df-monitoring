apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynatrace-api.fullname" . }}
  labels:
    {{- include "dynatrace-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "dynatrace-api.selectorLabels" . | nindent 4 }} 